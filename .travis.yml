language: rust
rust:
  - stable
  - beta
  - nightly
matrix:
  allow_failures:
    - rust: nightly
script: cargo test --features integration
notifications:
  email:
    - jesse.grillo@gmail.com
  on_failure: always
